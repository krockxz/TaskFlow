/**
 * Animated Components
 *
 * Exports only the actively used animation components.
 * All components are client-side only and include smooth transitions.
 */

// FadeIn - The only actively used animation component
export { FadeIn } from './fade-in';
export type { FadeInProps } from './fade-in';
